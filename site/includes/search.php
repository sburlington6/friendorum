<?php
//create a search

//the thing to be searched for in the data
$term = "Search Term";

//split it into words (split on space) add to an array
$words=array();

//split into letters (split at every character (remove spaces first))
$letters=array();

//compare data to words array first then to letters array

//show matched data (most words first then most letters)
?>